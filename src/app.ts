import { appContainer } from './inversify.config'

// to start with lets get the interface and just confirm building the following
// after buildnig the folowing then we do the scanning and results
// after the scanning and results we save to minio
// once all the above is working setup tasks to schedule it
// once tasking is setup, setup signal handling and clean up
// then stick it all in a container
// after it's in a container create a helm chart to manage it
 